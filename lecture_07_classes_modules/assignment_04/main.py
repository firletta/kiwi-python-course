# Create the module areas.py, where you will implement 3 functions
# rectangle_area(a, b), square_area(a), and circle_area(r).
# Each function will return corresponding area of the given rectangle/square/circle.
# Import this module to the main.py and print the output of all three functions with the arbitrary parameters.

import aeras
rectangle_area = aeras.rectangle_area(1, 2)
square_area = aeras.square_area(4)
circle_area = aeras.circle_area(5)

print(rectangle_area, square_area, circle_area)
