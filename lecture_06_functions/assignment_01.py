# Create an application with function, that will compute the triple of a given number. Call this function on 5 different integers and print the output.

def triples(number):
	return number*number*number # can be also **3

print(triples(5),triples(6),triples(8),triples(2),triples(1))